# 哈尔滨麻雀アプリ — Obsidian 連携・複数 AI 取扱説明

この文書は、**GitHub で同期している本リポジトリ**を Obsidian で開き、**複数の AI（Cursor / ChatGPT / Codex など）と情報を連携する**ための手順と運用ルールをまとめたものです。コード本体のデプロイ詳細は [[DEPLOY_FREE]] を参照してください（Obsidian 外から開く場合は `DEPLOY_FREE.md`）。

**ナレッジの玄関（全ノートへの入口）:** [[00-ホーム]]

**チャットで決めた方針の要約**（QA・テストプレイ・経緯）は [[これまでのまとめ]] を参照。

**ゲームディレクター用 DB（複数 AI の共通参照）:** [[ゲームDB/00_ゲームDBインデックス]]（目的・優先度・ルールのコード対応・決定ログ）

**「正」をどこに書くか（AI 同期）:** [[メモ/AI同期/ソースオブトゥルース]]

---

## 1. リポジトリで何ができるか（俯瞰）

| 種別 | 主なパス | 役割 |
|------|-----------|------|
| フロント（静的） | `index.html` → `harbin-mahjong.html` | ブラウザで動く麻雀 UI。Vercel 等にそのまま載せられる。 |
| アセット | `7198.png`、`tiles/`、ルートの `.png` | 牌画像など。ビルドで `public/` にコピーされる。 |
| オンライン用サーバ | `server/online-server.mjs` | Node の HTTP サーバ。Render 等で常駐させ、フロントから URL 指定で接続。 |
| ビルド | `npm run build` → `scripts/vercel-static-build.mjs` | Vercel 用に `public/` を生成。 |
| デプロイ手順 | `DEPLOY_FREE.md`、`render.yaml`、`vercel.json` | 無料枠でのホスト手順と設定。 |

ローカルでフロントだけ試す場合は、ルートの `index.html` をブラウザで開くか、簡易サーバでルートを配信してください。オンライン対戦を試す場合は `npm start` で `server/online-server.mjs` を起動し、ゲーム内の「サーバー URL 設定」にその URL を入れます（詳細は `DEPLOY_FREE.md`）。

---

## 2. Obsidian の「保管庫（Vault）」の開き方（重要）

**推奨構成（現在）:** リポジトリの**ルート**（`package.json` と同じ階層）に **`.obsidian/`** がある状態です。Obsidian から **そのルートフォルダを「保管庫として開く」** と、コード・画像・Markdown・設定が**ひとつの Vault** 内になり、リンク・検索・複数 AI への貼り付けが一貫します。

- **やること:** Obsidian → **保管庫としてフォルダを開く** → クローンした **`哈尔滨麻雀アプリ`（本リポ）のトップフォルダ** を指定する。

**旧構成について:** 以前は `ハルピン麻雀/.obsidian` だけがあり、Vault をサブフォルダにすると親の `server/` などが Vault 外になる問題がありました。設定は**ルートの `.obsidian` に集約済み**です。ウェルカム用の [[ハルピン麻雀/ようこそ]] は残してありますが、Vault ルートは必ずリポジトリ直下にしてください。

---

## 3. 複数 AI 連携のための運用（このリポジトリの使い方）

### 3.1 単一の「正」にする

- **仕様・決定事項・未解決バグ・本番 URL** は、原則 **このリポジトリ内の Markdown** に書き、GitHub に push する。  
- AI ごとのチャットは履歴が散らぶため、「確定したこと」だけをノートに抜き出す。

### 3.2 ノートの置き場所（例）

Vault ルートをリポジトリ直下にしたうえで、例えば次のようなフォルダを自分で作って運用すると整理しやすいです。

- `メモ/AI同期/` … どの AI からも同じ指示で読ませる前提のメモ（[[メモ/AI同期/ソースオブトゥルース]]）  
- `メモ/決定ログ/` … ADR 風の短い決定（なぜそうしたか一行＋リンク）  
- `メモ/バグ/` … 再現手順・環境・スクショのパス  
- `メモ/リリース/` … デプロイ日・接続先 URL・確認したコミット

ファイル名は **`YYYY-MM-DD_題名.md`** や **`題名_ステータス.md`** など、並べ替えしやすい規則にすると検索しやすいです。

### 3.3 AI に渡すときのコツ

- **相対パスでファイルを指す**：例「`AGENTS.md` と `ゲームDB/00_ゲームDBインデックス.md` を読んで」  
- **ゲーム仕様・優先度に触れるタスク**では、先に `ゲームDB/00_ゲームDBインデックス.md` の「読む順番」どおりに読ませる。  
- **Vault 内リンク**：Obsidian の `[[ノート名]]` は人間向け。AI にはファイルパスも併記する。  
- **長文は分割**：1 ノート 1 トピックにすると、差分とレビューが楽です。

### 3.4 Git と Obsidian の両立

- こまめに **commit / push** すると、別端末の AI や人間が同じ文脈に追いつけます。  
- **`.obsidian` はリポジトリに含める**（このプロジェクトではルートにコミット）。各自のレイアウト差は許容するか、個人用ブランチで調整してください。  
- 機密（トークン・パスワード）は **リポジトリに書かない**。環境変数やパスワードマネージャに置く。

---

## 4. よく使うコマンド（リポジトリルートで実行）

```bash
npm install          # 初回・依存変更時
npm run build        # public/ 生成（Vercel ビルドと同系）
npm start            # オンラインサーバ起動（デフォルト PORT はサーバ実装に依存）
```

Vercel / Render の手順は **`DEPLOY_FREE.md`** が詳しいです。

---

## 5. トラブルシューティング

| 現象 | 確認すること |
|------|----------------|
| Obsidian で `server/` が見えない | Vault がリポジトリ**直下**になっているか。§2 を参照。 |
| オンラインに繋がらない | フロントに設定したサーバ URL、`/api/health` の応答、Render のスリープ復帰待ち。 |
| ビルドで PNG が無いと言われる | `7198.png` がルートに存在するか（`scripts/vercel-static-build.mjs` が必須としている）。 |
| AI が古い仕様を参照する | ノートに「最終更新日」と「対象コミット hash」を書く習慣を付ける。 |

---

## 6. 関連ファイルへのリンク（Obsidian 用）

同一 Vault 内に次のファイルがある場合、Obsidian から開けます。

- [[00-ホーム]] — **全ナレッジの入口**  
- [[DEPLOY_FREE]] — 無料ホストでの公開手順（`DEPLOY_FREE.md`）  
- [[ゲームDB/00_ゲームDBインデックス]] — ディレクター DB（仕様・バックログ・AI 読順）  
- [[これまでのまとめ]] — QA（Codex）・経緯  
- `package.json` — npm スクリプト定義（エクスプローラまたは `Cmd/Ctrl+P` でファイル名検索）  
- [[ハルピン麻雀/ようこそ]] — 旧ウェルカムノート（ルート Vault では親も見える旨を記載）  
- [[AGENTS]] — Cursor 等のエージェント向け短文（パス指定用）

GitHub やエディタだけで見ている場合は、拡張子付きで `DEPLOY_FREE.md` などを開いてください。

---

## 7. 改訂履歴（手動で追記推奨）

| 日付 | 内容 |
|------|------|
| （記入） | 初版：Obsidian Vault 配置と複数 AI 運用 |
| 2026-05-11 | Vault をリポジトリルートに統一（`.obsidian` 移動）、[[00-ホーム]]・[[メモ/AI同期/ソースオブトゥルース]]・[[AGENTS.md]] 追加 |

---

*この取扱説明自体も「AI 共有メモ」として編集して構いません。重要な変更は Git にコミットしてください。*
